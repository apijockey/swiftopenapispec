openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvalidRef'
components:
  schemas:
    User:
      type: object
      properties:
        name:
          type: string
      required:
        - name
        - missingField
