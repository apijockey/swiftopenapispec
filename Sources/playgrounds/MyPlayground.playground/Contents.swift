import Cocoa

var greeting = "Hello, playground"
import SwiftOpenAPISpec

//let integerType = OpenAPIIntegerType(multipleOf: 2, maximum: 4, exclusiveMaximum: 1, minimum: 1, exclusiveMinimum: 2)
//let validatableType = OpenAPIValidatableSchemaTypes.integer(integerType)
//let jsonEncoder = JSONEncoder()
//let data = try jsonEncoder.encode(validatableType)
//print(String(data: data, encoding: .utf8))
